@{
    ApiMeals _apiMeals = new();
    IList<Category> categories = _apiMeals.GetMealCategories(new()).Result;
    ViewData["Title"] = "Meal Categories";
}

<div class="container mt-5">
    <h2>@ViewBag.title</h2>
    <div class="row">
        <div class="col-md-6 offset-md-3">
            <a class="btn btn-secondary" asp-action="Index">Home</a>
            @foreach (var category in categories)
            {
                <a class="btn btn-secondary" asp-action="MealCategory" asp-route-id="@category.strCategory">@category.strCategory</a>
            }
        </div>
    </div>
</div>
